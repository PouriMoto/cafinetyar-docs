# راهنمای اتصال — از Claude به پروژه MkDocs تو

این فایل توضیح می‌دهد داده‌های آماده‌شده را دقیقاً کجای پروژه‌ای که طبق راهنمای نصب ساختی بگذاری.

## آنچه از قبل برایت آماده شده

پوشه `mkdocs-ready/` شامل:
```
mkdocs-ready/
├── mkdocs.yml           ← جایگزین کن با mkdocs.yml که با `mkdocs new .` ساخته شد
└── docs/
    ├── index.md         ← صفحه اصلی سایت
    ├── stylesheets/
    │   └── rtl-fix.css  ← بهبود نمایش راست‌به‌چپ فارسی
    ├── core/            ← ۲۰ پوشه دسته، هرکدام شامل زیرپوشه‌های [ID]/index.md
    ├── edu/
    ├── tax/
    ├── customs/
    └── ... (۲۰ دسته کامل)
```

هر ۱۳۹ SOP فعلی از قبل تبدیل شده‌اند:
- نام فایل از `sop.md` به `index.md` تغییر کرده (طبق پیشنهاد نقشه‌راه)
- هر فایل یک front-matter دارد (title, category, code, priority, portal, season) — دقیقاً فرمتی که در پیام قبلی خواستی
- پوشه‌های خالی `images/` و `forms/` از قبل کنار هر SOP ساخته شده‌اند تا آماده افزودن تصویر/فرم در آینده باشند (طبق ساختاری که خودت پیشنهاد دادی)

## مراحل ادغام (بعد از گام ۵ نقشه‌راه، یعنی بعد از `mkdocs new .`)

1. پوشه `D:\CafeNetOS` را که با `mkdocs new .` ساختی باز کن — باید یک `docs/` (با فقط `index.md` داخلش) و یک `mkdocs.yml` ساده داشته باشد.
2. محتوای پوشه `mkdocs-ready/docs/` من را **جایگزین کامل** پوشه `docs/` خودت کن (یعنی `index.md` پیش‌فرض را با نسخه من عوض کن، و همه پوشه‌های دسته را داخل `docs/` بریز).
3. فایل `mkdocs-ready/mkdocs.yml` را جایگزین `mkdocs.yml` پیش‌فرض خودت کن.
4. دستور `mkdocs serve` را اجرا کن — باید بلافاصله همه ۱۳۹ خدمت را در منوی سمت راست ببینی.

## نکته مهم درباره فونت فارسی
فایل `rtl-fix.css` فرض کرده فونت Vazirmatn نصب یا از طریق CDN لود می‌شود. اگر فونت خراب/پیش‌فرض دیدی، این خط را به بالای `docs/index.md` یا در `mkdocs.yml` بخش `extra_css` اضافه کن تا فونت از گوگل‌فونت لود شود — بعداً اگر خواستی این را با هم درست می‌کنیم.

## هر بار که SOP جدید ساختیم
از این به بعد، من می‌توانم مستقیماً فایل را در همین فرمت (با front-matter و در پوشه دسته درست) بدهم — کافی است بگویی «طبق فرمت MkDocs بده» تا مستقیم قابل کپی در `docs/[دسته]/[ID]/index.md` باشد، بدون نیاز به تبدیل دوباره.
